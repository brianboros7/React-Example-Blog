import React from 'react'; 

function WithToggler(props) {
    const [open, setOpen] = useState(
        on: this.props.deafultOnValue 
    )

    toggle = () => {
        this.setState(prevState => {
            return (
                <div> 
                    on: !prevState.on
                </div>
            )
        })
    }

    return (
        <div> 
            
        </div>
    )
}

export default WithToggler  